services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_SHOW_FEATURES: "false"
    container_name: xylon-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - NODE_ENV=production

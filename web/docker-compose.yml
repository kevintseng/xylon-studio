services:
  web:
    build: .
    container_name: xylon-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - NODE_ENV=production
      - API_URL=http://localhost:5000
